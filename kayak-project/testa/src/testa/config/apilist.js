define('testa/config/apilist', function (require, exports, module) {
	var URL, URL1, CGI;
	URL = {
		//获取登录用户信息
		//getUserInfo: 'user/getUserInfo' //获取登录用户信息
	};
	URL1 = {}
	CGI = {};
	module.exports = $.extend({}, {
		URL: URL,
		URL1: URL1
	});
});
